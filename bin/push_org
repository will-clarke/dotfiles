#!/bin/bash
if [ ! -d /Users/wmmc/org ]; then
    echo "File not found!"
    exit 0
fi

cd /Users/wmmc/org
git add .
git commit -m "Automatic Changes from Cron"
HOME=/Users/wmmc git push origin master
