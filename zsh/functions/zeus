function rspec() {
  if [ -S .zeus.sock ]; then
    zeus rspec "$@"
  else
    command rspec "$@"
  fi
}

function rake() {
  if [ -S .zeus.sock ]; then
    zeus rake "$@"
  else
    command rake "$@"
  fi
}
